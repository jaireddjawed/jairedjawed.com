{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "start": "node index.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.2.2",
    "axios": "^0.24.0",
    "express": "^4.17.1",
    "yup": "^0.29.3"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
