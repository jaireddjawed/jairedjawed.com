{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm install && cd frontend && npm install && yarn run build && ../ && node index.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.2.2",
    "express": "^4.17.1",
    "yup": "^0.29.3"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
